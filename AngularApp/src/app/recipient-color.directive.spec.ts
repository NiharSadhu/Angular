import { RecipientColorDirective } from './recipient-color.directive';

describe('RecipientColorDirective', () => {
  it('should create an instance', () => {
    const directive = new RecipientColorDirective();
    expect(directive).toBeTruthy();
  });
});
